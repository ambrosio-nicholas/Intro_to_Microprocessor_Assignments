' {$STAMP BS2}
' {$PBASIC 2.5}
time VAR Word
inchDist VAR Word
cmDist VAR Word

DO
  PULSOUT 15, 5
  PULSIN 15, 1, time

  inchDist = time ** 890
  cmDist = time ** 2260
  DEBUG HOME, DEC3 ? inchDist, DEC3 ? cmDist

  IF inchDist < 8 THEN
    FREQOUT 11, 100, 4500
  ENDIF
  PAUSE 100
LOOP